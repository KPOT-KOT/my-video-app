<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
        body { background: #000; color: #fff; margin: 0; font-family: sans-serif; }
        .nav { padding: 15px; background: rgba(0,0,0,0.8); }
        .nav a { color: #3498db; text-decoration: none; }
        video { width: 100%; max-height: 80vh; outline: none; }
        .title { padding: 15px; text-align: center; }
    </style>
</head>
<body>
    <div class="nav">
        <a href="javascript:history.back()">⬅ Back to Folder</a>
    </div>

    <div class="title">
        <h3><%= filename %></h3>
    </div>

    <video controls playsinline autoplay>
        <source src="/video-stream?path=<%= encodedPath %>" type="video/mp4">
        Your browser does not support the video tag.
    </video>
</body>
</html>